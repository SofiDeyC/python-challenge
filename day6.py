# A dictionary for a user profile
user = {
    "name": "Sofia",
    "age": 14,
    "location": "London"
}

# Access values using keys
print(user["name"])   # Prints Sofia
print(user["age"])    # Prints 14

user["name"] = "Sophie"
print(user)
